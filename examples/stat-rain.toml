# Generated starter config for stat-rain.

[profiles.default.map]
speed = "cpu.normalized * 4 + 0.25"
density = "memory.normalized * 0.18 + 0.72"
color_hotness = "cpu.normalized"
brightness = "memory.normalized * 0.4 + 0.5"
fade_length = "cpu.normalized * 8 + 12"
glyph_churn = "cpu.normalized * 0.5 + 0.1"
message_reveal_intensity = "external.message_pressure.normalized"
ember_density = "0.0015"
ember_brightness = "0.9"
ember_color_hotness = "0"
ember_fade_length = "80"

[profiles.thermal.map]
speed = "cpu.normalized * 4 + 0.25"
density = "memory.normalized * 0.18 + 0.72"
color_hotness = "thermal_zone.raw / 100"
brightness = "cpu.normalized * 0.3 + 0.6"
fade_length = "memory.normalized * 10 + 12"
glyph_churn = "cpu.normalized * 0.35 + 0.1"
message_reveal_intensity = "external.message_pressure.normalized"
ember_density = "cpu.normalized * 0.0015 + 0.001"
ember_brightness = "memory.normalized * 0.25 + 0.65"
ember_color_hotness = "thermal_zone.raw / 100"
ember_fade_length = "thermal_zone.normalized * 24 + 72"

[profiles.system_activity.map]
speed = "cpu.normalized * 4 + 0.25"
density = "memory.normalized * 0.18 + 0.72"
color_hotness = "cpu.normalized"
brightness = "network_io.normalized * 0.35 + 0.55"
fade_length = "disk_io.normalized * 10 + 12"
glyph_churn = "network_io.normalized * 0.45 + 0.1"
message_reveal_intensity = "external.message_pressure.normalized"
ember_density = "disk_io.normalized * 0.002 + 0.001"
ember_brightness = "memory.normalized * 0.25 + 0.65"
ember_color_hotness = "cpu.normalized"
ember_fade_length = "network_io.normalized * 24 + 72"
